# 测试操作指南

## 运行场景

**场景文件：** `samples/scenes/room_layout_2d_sample.tscn`

这是已有的 2D 示例场景，包含完整的生成管线。运行后界面右下角有 **Generate** 按钮，点击即可生成地图。

---

## 测试步骤

### 第一步：在 Godot 中打开项目，确认编译通过

1. 用 Godot 4.6 打开 `ManiaMapGodot` 项目
2. 等待 .NET 编译完成（底部状态栏会显示 Build 状态）
3. 如果报错，先在命令行 `dotnet build` 确认无问题

### 第二步：修改布局图，配置重点房间

1. 在 FileSystem 面板中，双击打开 `samples/2d_rooms/graphs/cross_graph_2d.tres`
2. 底部会出现 **Layout Graph** 编辑器面板，显示 6 个节点的十字形图：

```
       Node 2
         |
Node 1—Node 3—Node 5—Node 6
         |
       Node 4
```

3. **配置出生房（Node 1）：**
   - 在图编辑器中点击选中 Node 1
   - 右侧 Inspector 面板中修改：
     - `Name` → `Spawn`
     - `Tags` → 添加一个元素，输入 `spawn`
     - `PositionConstraint` → 下拉选择 `Bottom`
     - `Color` → 改为绿色（可选，用于图编辑器中区分）

4. **配置撤离房（Node 6）：**
   - 点击选中 Node 6
   - Inspector 中修改：
     - `Name` → `Exit`
     - `Tags` → 添加 `extraction`
     - `PositionConstraint` → 下拉选择 `TopEdge`
     - `Color` → 改为红色（可选）

5. 保存资源（Ctrl+S）

### 第三步：配置地图约束参数（可选）

1. 在场景树中展开：`RoomLayout2DSample → GenerationPipeline → Steps → LayoutGeneratorStep`
2. 选中 `LayoutGeneratorStep` 节点
3. Inspector 中会看到新增的 **Map Constraints** 分组：
   - `Max Map Width` → 设为 `30`（期望地图最大 30 列，0=不限）
   - `Max Map Height` → 设为 `30`（期望地图最大 30 行）
   - `Map Size Tolerance` → 保持默认 `3`（允许超出 3 格）
   - `Distance Constraints` → 可添加元素，每个元素是 Dictionary：
     - 点击 "Add Element"
     - 展开新元素，添加三个 key：
       - `TagA` (String) = `spawn`
       - `TagB` (String) = `extraction`
       - `MinDistance` (Int) = `5`

### 第四步：运行测试

1. 按 **F5** 运行（或点击右上角的 ▶ 按钮）
2. 场景启动后，点击右下角的 **Generate** 按钮
3. 观察生成结果：
   - 出生房应有 **绿色 "SPAWN" 标签** + 浅绿色底色
   - 撤离房应有 **红色 "EXIT" 标签** + 浅红色底色
   - 出生房应在地图**底部**（画面下方）
   - 撤离房应在地图**顶部边缘**（画面上方）
4. 多次点击 Generate 验证：
   - 每次生成的布局不同
   - 但出生房始终在底部，撤离房始终在顶部

### 操作说明

- **拖拽平移**：鼠标左键拖拽
- **缩放**：鼠标滚轮

---

## 快速对照：现有图的 6 个节点

| 节点 | 建议配置 | PositionConstraint | Tags |
|------|---------|-------------------|------|
| Node 1 | 出生房 | `Bottom` | `["spawn"]` |
| Node 2 | 普通房 | `None` | `[]` |
| Node 3 | 普通房(中心) | `None` | `[]` |
| Node 4 | 普通房 | `None` | `[]` |
| Node 5 | 普通房 | `None` | `[]` |
| Node 6 | 撤离房 | `TopEdge` | `["extraction"]` |

---

## 如果生成失败（FAILED）

可能原因和解决办法：
- 约束过于严格 → 减小 MinDistance 值，增大 Map Size 或去掉尺寸限制（设为 0）
- 房间模板太少 → 当前只有 `square_3x3` 一种模板，候选方向有限

## 验证要点

| 检查项 | 预期结果 |
|--------|----------|
| SPAWN 标签 | 绿色文字出现在出生房上 |
| EXIT 标签 | 红色文字出现在撤离房上 |
| 出生房位置 | 地图最下方区域 |
| 撤离房位置 | 地图上半部且在边缘 |
| 多次生成 | 布局不同但约束始终满足 |
